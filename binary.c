#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
void binary(int);

int main()
{

    int num;
    printf("Enter a number: ");
    scanf("%d",&num);
    printf("\n Binary equivalent of number is ");
    binary(num);
    getch(;
    )
}